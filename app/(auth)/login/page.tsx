"use client";

import { useEffect,useState } from "react";
import { useRouter } from "next/navigation";
import { ArrowRight,Eye,EyeOff,LockKeyhole,Mail,ShieldCheck,UserPlus } from "lucide-react";
import { toast } from "sonner"; import { supabase } from "@/lib/supabase"; import { DanchristaLogo } from "@/components/brand/DanchristaLogo";
type AuthMode="login"|"verify"|"reset"|"forgot"|"mfa"|"staff_signup";
export default function LoginPage(){const router=useRouter();const[mode,setMode]=useState<AuthMode>("login");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[newPassword,setNewPassword]=useState("");const[showPassword,setShowPassword]=useState(false);const[loading,setLoading]=useState(false);const[authError,setAuthError]=useState("");const[mfaCode,setMfaCode]=useState("");const[mfaFactorId,setMfaFactorId]=useState("");
function getAuthRedirectUrl(){return `${window.location.origin}/login`;} function getAuthUrlValue(name:string){const s=new URLSearchParams(window.location.search);const h=window.location.hash.startsWith("#")?window.location.hash.slice(1):window.location.hash;const hp=new URLSearchParams(h);return s.get(name)??hp.get(name);} function hasRecoverySignal(){return getAuthUrlValue("type")==="recovery";} function showCallbackError(){const error=getAuthUrlValue("error_description")??getAuthUrlValue("error");if(!error)return;const friendlyError=decodeURIComponent(error.replace(/\+/g," "));setAuthError(friendlyError.toLowerCase().includes("expired")||friendlyError.toLowerCase().includes("invalid")?"That link is invalid or has expired. Request a new one and try again.":friendlyError);}
async function ensureCompanyThenRedirect(){const{data:{user}}=await supabase.auth.getUser();if(!user){setLoading(false);return;}const{data:profile}=await supabase.from("profiles").select("company_id,is_active").eq("id",user.id).maybeSingle();if(profile?.company_id&&profile.is_active!==false){setLoading(false);router.replace("/dashboard");return;}const{data:joinedCompanyId,error:inviteError}=await supabase.rpc("accept_staff_invitation");setLoading(false);if(!inviteError&&joinedCompanyId){toast.success("Welcome to the Amezing Limited team!");router.replace("/dashboard");return;}await supabase.auth.signOut();setAuthError("This email does not have a valid Amezing Limited staff invitation. Ask the Amezing Limited owner to add you first.");}
useEffect(()=>{showCallbackError();if(hasRecoverySignal())setMode("reset");const{data:{subscription}}=supabase.auth.onAuthStateChange(event=>{if(event==="PASSWORD_RECOVERY"){setAuthError("");setMode("reset");}});return()=>subscription.unsubscribe();},[]);
async function resendVerification(){if(!email.trim()){toast.error("Enter your email first.");return;}setLoading(true);const{error}=await supabase.auth.resend({type:"signup",email:email.trim(),options:{emailRedirectTo:getAuthRedirectUrl()}});setLoading(false);if(error){toast.error(error.message);return;}toast.success("Verification email sent again.");}
async function handleStaffSignup(e:React.FormEvent){e.preventDefault();if(!email.trim()||!password.trim()){toast.error("Email and password are required.");return;}if(password.length<8){toast.error("Use at least 8 characters for your password.");return;}setAuthError("");setLoading(true);const{data,error}=await supabase.auth.signUp({email:email.trim().toLowerCase(),password,options:{emailRedirectTo:getAuthRedirectUrl()}});if(error){setLoading(false);toast.error(error.message);return;}if(!data.session){setLoading(false);setMode("verify");toast.success("Check your email to verify the staff account, then return here to sign in.");return;}await ensureCompanyThenRedirect();}
async function handleForgotPassword(e:React.FormEvent){e.preventDefault();if(!email.trim()){toast.error("Enter your account email first.");return;}setLoading(true);const{error}=await supabase.auth.resetPasswordForEmail(email.trim(),{redirectTo:getAuthRedirectUrl()});setLoading(false);if(error){toast.error(error.message);return;}toast.success("Password reset email sent.");}
async function handleResetPassword(e:React.FormEvent){e.preventDefault();if(newPassword.length<8){toast.error("Use at least 8 characters for your new password.");return;}setLoading(true);const{error}=await supabase.auth.updateUser({password:newPassword});setLoading(false);if(error){toast.error(error.message);return;}toast.success("Password updated. Welcome back.");setNewPassword("");await ensureCompanyThenRedirect();}
async function handleSubmit(e:React.FormEvent){e.preventDefault();if(!email.trim()||!password.trim()){toast.error("Email and password are required.");return;}setAuthError("");setLoading(true);const{error}=await supabase.auth.signInWithPassword({email:email.trim(),password});if(error){if(error.message.toLowerCase().includes("email not confirmed")){setLoading(false);setMode("verify");return;}toast.error("Unable to sign in. Check your email and password.");setLoading(false);return;}const{data:aal}=await supabase.auth.mfa.getAuthenticatorAssuranceLevel();if(aal?.nextLevel==="aal2"&&aal.nextLevel!==aal.currentLevel){const{data:factors}=await supabase.auth.mfa.listFactors();const factor=factors?.totp.find(f=>f.status==="verified");if(factor){setMfaFactorId(factor.id);setLoading(false);setMode("mfa");return;}}await ensureCompanyThenRedirect();}
async function handleMfaVerify(e:React.FormEvent){e.preventDefault();if(mfaCode.trim().length<6){toast.error("Enter the 6-digit code from your authenticator app.");return;}setLoading(true);const{data:challenge,error:challengeError}=await supabase.auth.mfa.challenge({factorId:mfaFactorId});if(challengeError){setLoading(false);toast.error(challengeError.message);return;}const{error:verifyError}=await supabase.auth.mfa.verify({factorId:mfaFactorId,challengeId:challenge.id,code:mfaCode.trim()});if(verifyError){setLoading(false);toast.error("That code didn't match. Try again.");return;}await ensureCompanyThenRedirect();}
if(mode==="mfa")return <AuthShell><DanchristaLogo dark/><div className="mx-auto mt-8 grid size-14 place-items-center rounded-2xl bg-[#c98a4f]/10 text-[#e7b784]"><ShieldCheck className="size-6"/></div><h1 className="mt-6 text-center font-heading text-2xl font-bold tracking-tight">Enter your code</h1><p className="mt-3 text-center text-sm leading-6 text-[#93a69c]">Open your authenticator app and enter the 6-digit code for this account.</p><form onSubmit={handleMfaVerify} className="mt-7 space-y-4"><input type="text" inputMode="numeric" autoFocus maxLength={6} placeholder="123456" value={mfaCode} onChange={e=>setMfaCode(e.target.value)} className="w-full rounded-lg border border-white/10 bg-white/5 py-3 text-center text-lg tracking-[0.4em] text-[#f4f1ea] focus:border-[#c98a4f]/60 focus:outline-none focus:ring-2 focus:ring-[#c98a4f]/20"/><Submit loading={loading} label="Verify" loadingLabel="Verifying…"/></form><BackButton onClick={()=>{setMode("login");setMfaCode("");}}/></AuthShell>;
if(mode==="verify")return <AuthShell><DanchristaLogo dark/><div className="mx-auto mt-8 grid size-14 place-items-center rounded-2xl bg-[#c98a4f]/10 text-[#e7b784]"><Mail className="size-6"/></div><h1 className="mt-6 text-center font-heading text-2xl font-bold tracking-tight">Check your email</h1><p className="mt-3 text-center text-sm leading-6 text-[#93a69c]">We sent a verification link to <strong className="font-semibold text-[#f4f1ea]">{email}</strong>.</p>{authError&&<ErrorMessage>{authError}</ErrorMessage>}<button type="button" onClick={resendVerification} disabled={loading} className="mt-7 w-full rounded-lg bg-[#c98a4f] px-5 py-3.5 text-sm font-semibold text-[#1a1008] transition hover:bg-[#d69a61] disabled:opacity-50">{loading?"Sending…":"Resend verification email"}</button><BackButton onClick={()=>{setAuthError("");setMode("login");}}/></AuthShell>;
if(mode==="forgot")return <AuthShell><DanchristaLogo dark/><div className="mt-8"><p className="font-mono text-[11px] text-[#e7b784]">Account recovery</p><h1 className="mt-2 font-heading text-2xl font-bold tracking-tight">Reset your password</h1><p className="mt-3 text-sm leading-6 text-[#93a69c]">Enter your account email and we&apos;ll send a secure reset link.</p></div>{authError&&<ErrorMessage>{authError}</ErrorMessage>}<form onSubmit={handleForgotPassword} className="mt-7 space-y-4"><FieldLabel label="Email"><EmailField value={email} onChange={setEmail} autoFocus/></FieldLabel><Submit loading={loading} label="Send reset link" loadingLabel="Sending reset link…" noArrow/></form><button type="button" onClick={()=>{setAuthError("");setMode("login");}} className="mt-5 w-full text-center text-sm font-medium text-[#93a69c] hover:text-[#f4f1ea]">Back to sign in</button></AuthShell>;
if(mode==="reset")return <AuthShell><DanchristaLogo dark/><div className="mx-auto mt-8 grid size-14 place-items-center rounded-2xl bg-[#c98a4f]/10 text-[#e7b784]"><LockKeyhole className="size-6"/></div><h1 className="mt-6 text-center font-heading text-2xl font-bold tracking-tight">Choose a new password</h1><p className="mt-3 text-center text-sm leading-6 text-[#93a69c]">Use at least 8 characters, then you&apos;ll go straight back to your shop.</p><form onSubmit={handleResetPassword} className="mt-7 space-y-4"><FieldLabel label="New password"><PasswordField value={newPassword} onChange={setNewPassword} show={showPassword} onToggle={()=>setShowPassword(v=>!v)} autoComplete="new-password"/></FieldLabel><Submit loading={loading} label="Update password" loadingLabel="Updating password…"/></form></AuthShell>;
if(mode==="staff_signup")return <AuthShell><DanchristaLogo dark/><div className="mt-8"><p className="font-mono text-[11px] text-[#e7b784]">Staff invitation</p><h1 className="mt-2 font-heading text-2xl font-bold tracking-tight">Create your Amezing Limited access.</h1><p className="mt-3 text-sm leading-6 text-[#93a69c]">Use the same email the Amezing Limited owner added. You can only enter the shop after the invitation is accepted.</p></div>{authError&&<ErrorMessage>{authError}</ErrorMessage>}<form onSubmit={handleStaffSignup} className="mt-7 space-y-4"><FieldLabel label="Email"><EmailField value={email} onChange={setEmail} autoFocus/></FieldLabel><FieldLabel label="Password"><PasswordField value={password} onChange={setPassword} show={showPassword} onToggle={()=>setShowPassword(v=>!v)} autoComplete="new-password"/></FieldLabel><Submit loading={loading} label="Create staff access" loadingLabel="Creating access…"/></form><button type="button" onClick={()=>{setAuthError("");setMode("login");}} className="mt-5 w-full text-center text-sm font-medium text-[#93a69c] hover:text-[#f4f1ea]">Back to sign in</button></AuthShell>;
return <AuthShell><DanchristaLogo dark/><div className="mt-8"><p className="font-mono text-[11px] text-[#e7b784]">Private business system</p><h1 className="mt-2 font-heading text-2xl font-bold tracking-tight sm:text-3xl">Sign in to Amezing Limited.</h1><p className="mt-3 text-sm leading-6 text-[#93a69c]">Only the Amezing Limited owner and approved staff can enter the workspace.</p></div>{authError&&<ErrorMessage>{authError}</ErrorMessage>}<form onSubmit={handleSubmit} className="mt-7 space-y-4"><FieldLabel label="Email"><EmailField value={email} onChange={setEmail}/></FieldLabel><FieldLabel label="Password"><PasswordField value={password} onChange={setPassword} show={showPassword} onToggle={()=>setShowPassword(v=>!v)} autoComplete="current-password"/></FieldLabel><div className="flex justify-end"><button type="button" onClick={()=>setMode("forgot")} className="text-xs font-medium text-[#e7b784] hover:text-[#f0c398]">Forgot password?</button></div><Submit loading={loading} label="Sign in" loadingLabel="Signing in…"/></form><button type="button" onClick={()=>{setAuthError("");setMode("staff_signup");}} className="mt-6 flex w-full items-center justify-center gap-2 rounded-lg border border-white/10 px-5 py-3.5 text-sm font-medium text-[#c7d1cb] transition hover:border-white/25"><UserPlus className="size-4 text-[#e7b784]"/>Have a staff invitation?</button><div className="mt-7 flex items-center gap-3 text-[10px] text-[#5c6b64]"><div className="h-px flex-1 bg-white/10"/>secure workspace<div className="h-px flex-1 bg-white/10"/></div><div className="mt-4 flex items-center justify-center gap-2 text-xs text-[#93a69c]"><ShieldCheck className="size-4 text-[#e7b784]"/>Access is controlled by your Amezing Limited role.</div></AuthShell>;}
function AuthShell({children}:{children:React.ReactNode}){return <main className="min-h-screen bg-[#0b1512] px-5 py-10 text-[#f4f1ea]"><div className="mx-auto flex min-h-[85vh] max-w-md flex-col justify-center"><div className="rounded-2xl border border-[#c98a4f]/15 bg-[#101f1a] p-8 sm:p-10">{children}</div></div></main>;} function ErrorMessage({children}:{children:React.ReactNode}){return <p className="mt-4 rounded-lg border border-red-500/25 bg-red-500/10 px-4 py-3 text-center text-xs leading-5 text-red-300">{children}</p>;} function EmailField({value,onChange,autoFocus}:{value:string;onChange:(value:string)=>void;autoFocus?:boolean}){return <div className="relative"><Mail className="pointer-events-none absolute left-3.5 top-1/2 size-4 -translate-y-1/2 text-[#6b7d74]"/><input type="email" autoFocus={autoFocus} autoComplete="email" placeholder="you@example.com" value={value} onChange={e=>onChange(e.target.value)} className="w-full rounded-lg border border-white/10 bg-white/5 py-3 pl-11 pr-4 text-sm text-[#f4f1ea] placeholder:text-[#6b7d74] focus:border-[#c98a4f]/60 focus:outline-none focus:ring-2 focus:ring-[#c98a4f]/20"/></div>;} function PasswordField({value,onChange,show,onToggle,autoComplete}:{value:string;onChange:(value:string)=>void;show:boolean;onToggle:()=>void;autoComplete:string}){return <div className="relative"><LockKeyhole className="pointer-events-none absolute left-3.5 top-1/2 size-4 -translate-y-1/2 text-[#6b7d74]"/><input type={show?"text":"password"} autoComplete={autoComplete} placeholder="Your password" value={value} onChange={e=>onChange(e.target.value)} className="w-full rounded-lg border border-white/10 bg-white/5 py-3 pl-11 pr-11 text-sm text-[#f4f1ea] placeholder:text-[#6b7d74] focus:border-[#c98a4f]/60 focus:outline-none focus:ring-2 focus:ring-[#c98a4f]/20"/><button type="button" onClick={onToggle} className="absolute right-3 top-1/2 -translate-y-1/2 rounded-md p-1.5 text-[#6b7d74] hover:bg-white/5 hover:text-[#f4f1ea]" aria-label={show?"Hide password":"Show password"}>{show?<EyeOff className="size-4"/>:<Eye className="size-4"/>}</button></div>;} function FieldLabel({label,children}:{label:string;children:React.ReactNode}){return <label className="block text-sm font-medium text-[#c7d1cb]">{label}<div className="mt-1.5">{children}</div></label>;} function Submit({loading,label,loadingLabel,noArrow}:{loading:boolean;label:string;loadingLabel:string;noArrow?:boolean}){return <button type="submit" disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-lg bg-[#c98a4f] px-5 py-3.5 text-sm font-semibold text-[#1a1008] transition hover:bg-[#d69a61] disabled:opacity-50">{loading?loadingLabel:label}{!loading&&!noArrow&&<ArrowRight className="size-4"/>}</button>;} function BackButton({onClick}:{onClick:()=>void}){return <button type="button" onClick={onClick} className="mt-3 w-full rounded-lg border border-white/10 px-5 py-3.5 text-sm font-medium text-[#c7d1cb] transition hover:border-white/25">Back to sign in</button>;}
